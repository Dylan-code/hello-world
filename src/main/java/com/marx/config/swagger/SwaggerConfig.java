package com.marx.config.swagger;
/*
import org.springframework.context.annotation.Bean;
import org.springframework.context.annotation.Configuration;
import org.springframework.stereotype.Controller;
import springfox.documentation.builders.RequestHandlerSelectors;
import springfox.documentation.service.ApiInfo;
import springfox.documentation.spi.DocumentationType;
import springfox.documentation.spring.web.plugins.Docket;
import springfox.documentation.swagger2.annotations.EnableSwagger2;

@Configuration
@EnableSwagger2
public class SwaggerConfig {

    @Bean
    public Docket docket1() {
        Docket docket = new Docket(DocumentationType.SWAGGER_2);
        docket.apiInfo(apiInfo()).groupName("汪以平");
        docket.select().apis(RequestHandlerSelectors.basePackage("com.marx.controller")).build();
//        docket.select().apis(RequestHandlerSelectors.withMethodAnnotation(ApiOperation.class));
//        docket.select().apis(RequestHandlerSelectors.withClassAnnotation(Controller.class));
        return docket;
    }

    public ApiInfo apiInfo() {
        ApiInfo apiInfo = ApiInfo.DEFAULT;

        return apiInfo;
    }

}
*/